{
  "builds": [
    {
      "src": "index.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.12"
      }
    }
  ],
  "build": {
    "env": {
      "PIP_NO_CACHE_DIR": "false"
    },
    "commands": [
      "pip install --upgrade pip setuptools wheel"
    ]
  }
}
